syntax = "proto3";

package main;

option go_package = "perftest/proto";

service Perftest {
  rpc CountWords(StringRequest) returns (IntResponse);
  rpc CountWordsStream(stream StringRequest) returns (IntResponse);
}

message StringRequest {
  string content = 1;
}

message IntResponse {
  int64 value = 1;
}