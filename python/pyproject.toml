[project]
name = "perftest"
version = "0.1.0"
description = "Performance test of Go usage in Python"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "grpcio>=1.76.0",
    "grpcio-tools>=1.76.0",
    "pytest>=9.0.2",
]
